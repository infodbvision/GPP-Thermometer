extends layout
block content
	.container
		h3 Betaal Check
		p Dit zijn uw order details:
		p Punt(en): #{payment.description}
		p Prijs: €#{payment.amount}

		a(class='w3-btn w3-ripple w3-indigo' style='border-radius: 10px' href=payment.links.paymentUrl) Betaal met iDeal
